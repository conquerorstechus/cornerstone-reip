# Optional. Production: https://reip.csdigitaltech.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# n8n webhook for Get more info (name, email, phone + deal).
LEAD_WEBHOOK_URL=https://n8n.srv1393511.hstgr.cloud/webhook/form-submission

# Optional override for the listings JSON. Default is the n8n latest-listings webhook.
# Cached for 1 day in the Next.js data cache (refetched on a later request, or via the
# hidden refresh control in the site footer).
# SAM_LIST_URL=https://n8n.srv1393511.hstgr.cloud/webhook/latest-listings
